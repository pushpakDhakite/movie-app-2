import { cn } from '@/utils/format';
import { ScrollArea as ScrollAreaPrimitive, ScrollBar as ScrollBarPrimitive } from '@/components/ui/scroll-area-primitive';

export const ScrollArea = ScrollAreaPrimitive;
export const ScrollBar = ScrollBarPrimitive;
